<?xml version="1.0" encoding="ISO-8859-1"?>
<binary>
<project>base</project>
<name>scalexml</name>
<location>src.bin/graphic/scalexml</location>
<syntax>scalexml --help</syntax>
<syntax>scalexml [-smooth] [-wdt <ar>width</ar>] [-hgt <ar>height</ar>] [<ar>name</ar>]</syntax>

<option><on>--help</on><od>print the help message and exit.</od>
</option>
<option><on>-smooth</on><od>smooth the output image.</od>
</option>
<option><on>-wdt <ar>width</ar></on><od>make the output image width pixels wide.</od>
</option>
<option><on>-hgt <ar>height</ar></on><od>make the output image height pixels tall.</od>
</option>
<option><on><ar>name</ar></on><od>input image filename. If this is omitted the image  will be read from standard input.</od>
</option>
<synopsis><p>Scale an XML representation of an image.</p></synopsis>



<description><p>Scale an XML representation of an image.</p>
<p>The scaled XML image is written to standard output.</p></description>

<example>
<command>scalexml -wdt 256 -hgt 256 inp.xml &gt; out.xml</command>
<description>Scale the XML file <code>inp.xml</code> to 256x256 pixels and store it in the file <code>out.xml</code>.
</description>
</example>

<example>
<command>scalexml -smooth -wdt 64 -hgt 64 inp.xml &gt; out.xml</command>
<description>Scale the XML file <code>inp.xml</code> to 64x64 pixels with smoothing and store it in the file  <code>out.xml</code>.
</description>
</example>



</binary>
