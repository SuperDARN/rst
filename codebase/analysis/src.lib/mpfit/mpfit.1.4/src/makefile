# Makefile for mpfit library
# ===========================
include $(MAKECFG).$(SYSTEM)

INCLUDE=-I$(IPATH)/analysis

SRC = mpfit.c mpfit.h
OBJS = mpfit.o

INC=$(IPATH)/analysis
DSTPATH=$(LIBPATH)
OUTPUT = mpfit
LINK="1"

include $(MAKELIB).$(SYSTEM)

