<?xml version="1.0" encoding="ISO-8859-1"?>
<binary>
<project>base</project>
<name>scaleppmx</name>
<location>src.bin/graphic/scaleppmx</location>
<syntax>scaleppmx --help</syntax>
<syntax>scaleppmx [-smooth] [-wdt <ar>width</ar>] [-hgt <ar>height</ar>] [<ar>name</ar>]</syntax>

<option><on>--help</on><od>print the help message and exit.</od>
</option>
<option><on>-smooth</on><od>smooth the output image.</od>
</option>
<option><on>-wdt <ar>width</ar></on><od>make the output image width pixels wide.</od>
</option>
<option><on>-hgt <ar>height</ar></on><od>make the output image height pixels tall.</od>
</option>
<option><on><ar>name</ar></on><od>input image filename. If this is omitted the image  will be read from standard input.</od>
</option>
<synopsis><p>Scale an extended Portable PixMap (PPMX) image.</p></synopsis>

<description><p>Scale an extended Portable PixMap (PPMX) image.</p>
<p>The scaled PPMX image file is written to standard output.</p></description>

<example>
<command>scaleppmx -wdt 256 -hgt 256 inp.ppmx &gt; out.ppmx</command>
<description>Scale the PPMX file <code>inp.ppmx</code> to 256x256 pixels and store it in the file <code>out.ppmx</code>.
</description>
</example>

<example>
<command>scaleppm -smooth -wdt 64 -hgt 64 inp.ppmx &gt; out.ppmx</command>
<description>Scale the PPMX file <code>inp.ppmx</code> to 64x64 pixels with smoothing and store it in the file <code>out.ppmx</code>.
</description>
</example>



</binary>
