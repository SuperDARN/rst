#  Makefile for makeall
#  ====================
include $(MAKECFG).$(SYSTEM)

INCLUDE=

OBJS = makeall.o
SRC=makeall.c hlpstr.h errstr.h 
DSTPATH = $(BINPATH)
OUTPUT = makeall
LIBS= 
SLIB= -lm


 















include $(MAKEBIN).$(SYSTEM)
