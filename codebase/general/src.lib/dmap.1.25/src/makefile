# Makefile for dmap library
# =========================
# by R.J.Barnes
#
#
include $(MAKECFG).$(SYSTEM)
INCLUDE=-I$(IPATH)/base -I$(IPATH)/general
SRC=dmap.c merge.c fp.c
OBJS = dmap.o merge.o fp.o
INC=$(IPATH)/general
DSTPATH=$(LIBPATH)
OUTPUT = dmap
LINK="1"

















include $(MAKELIB).$(SYSTEM)
